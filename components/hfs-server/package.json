{
  "name": "hfs-server",
  "version": "1.0.0",
  "main": "src/server.js",
  "repository": {},
  "scripts": {
    "test": "../../node_modules/.bin/mocha -R dot 'test/**/*.test.js'",
    "start": "node bin/server --config config/local.json"
  }
}
