#!/bin/sh

set -e

cd /var/src/service-core/components/previews-server

export NODE_ENV=production
export NODE_PATH=/var/src/service-core
exec chpst -u app /usr/bin/node src/server --config $PREVIEWS_CONFIG
