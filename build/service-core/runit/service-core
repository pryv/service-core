#!/bin/sh

set -e

cd /var/pryv/service-core/components/api-server

export NODE_ENV=production
export NODE_PATH=/var/pryv/service-core
exec chpst -u app /usr/bin/node src/server --config production.json
