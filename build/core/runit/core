#!/bin/sh

set -e

cd /app/bin/components/api-server

chown -R app:app /app/data/

export NODE_ENV=production
export NODE_PATH=/app/bin/
exec chpst -u app /usr/bin/node src/server --config /app/conf/core.json
