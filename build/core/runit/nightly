#!/bin/sh

set -e

cd /app/bin/dist/components/api-server

chown -R app:app /app/data/
chown -R app:app /app/log/

# Setup nightly cron-tasks
exec chpst -u app ./bin/nightly  --config /app/conf/core.json