#!/usr/bin/env bash
# This file installs all dependencies needed to run core for tests

# Install nats-server - the duplicate of ./core/nats-server.sh file that consists of docker
# run commands
sh ./scripts/setup-nats-server.sh

# Install mongodb
bash ./scripts/setup-dev-env.bash

# Install influxdb
sh ./scripts/setup-influx.sh

# Install graphicsmagick for preview functionality
sudo apt-get install graphicsmagick

#======================================================================
# If the script is run several times, this could fail because of occupied ports
# but in the isolated environment like docker it is ok, but for local machine
# installation could cause confusion

# Start nats-server (the same as `npm run nats`, just in the background)
./nats-server/nats-server-v2.3.4-linux-amd64/nats-server &

# Start Influx db and verify the status
sudo service influxdb start

# Start mongodb
npm run database
