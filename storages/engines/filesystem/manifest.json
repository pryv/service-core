{
  "storageTypes": ["fileStorage"],
  "entrypoint": "src/index.js",
  "requiredInternals": [
    "userLocalDirectory",
    "getLogger",
    "eventFilesConfig"
  ],
  "configuration": {
    "configKey": "eventFiles",
    "fields": {
      "attachmentsDirPath":  { "type": "string",  "required": true,  "description": "Directory for event file attachments" },
      "previewsDirPath":     { "type": "string",  "required": true,  "description": "Directory for event file previews" }
    }
  }
}
